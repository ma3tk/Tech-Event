import { Meta, Unstyled } from "@storybook/addon-docs/blocks";

<Meta title="Welcome" />

<Unstyled>
  <div
    style={{
      fontFamily: "system-ui, 'Noto Sans JP', 'Hiragino Kaku Gothic ProN', sans-serif",
      color: "#1a1a1a",
      lineHeight: 1.6,
      maxWidth: "1080px",
      margin: "0 auto",
      padding: "0 8px 48px",
    }}
  >
    {/* ============ Hero ============ */}
    <div
      style={{
        marginTop: "8px",
        padding: "48px 40px 56px",
        borderRadius: "12px",
        background:
          "linear-gradient(135deg, #c2410c 0%, #9a3412 60%, #7c2d12 100%)",
        color: "#ffffff",
        boxShadow: "0 1px 2px rgba(0,0,0,0.06), 0 8px 24px rgba(194,65,12,0.18)",
        position: "relative",
        overflow: "hidden",
      }}
    >
      <div
        style={{
          position: "absolute",
          inset: 0,
          background:
            "radial-gradient(800px 200px at 90% -20%, rgba(255,255,255,0.18), transparent 60%)",
          pointerEvents: "none",
        }}
      />
      <div style={{ position: "relative" }}>
        <div
          style={{
            display: "inline-flex",
            alignItems: "center",
            gap: "8px",
            padding: "6px 10px",
            borderRadius: "9999px",
            background: "rgba(255,255,255,0.14)",
            border: "1px solid rgba(255,255,255,0.28)",
            fontSize: "12px",
            fontWeight: 600,
            letterSpacing: "0.04em",
            marginBottom: "20px",
          }}
        >
          <span
            style={{
              width: "6px",
              height: "6px",
              borderRadius: "9999px",
              background: "#fff7ed",
              boxShadow: "0 0 0 4px rgba(255,247,237,0.25)",
            }}
          />
          Design System v1.0 / Storybook 10
        </div>
        <h1
          style={{
            margin: 0,
            fontSize: "44px",
            lineHeight: 1.15,
            fontWeight: 800,
            letterSpacing: "-0.01em",
          }}
        >
          tech-event Design System
        </h1>
        <p
          style={{
            margin: "12px 0 0",
            fontSize: "18px",
            opacity: 0.92,
            maxWidth: "720px",
          }}
        >
          connpass と Luma の中間 — 機能密度と質感を両立する UI 基盤。
          <br />
          トークン / Primitives / Composite components / アクセシビリティ規約を
          1 箇所から横断できる総合カタログ。
        </p>
        <div
          style={{
            display: "flex",
            gap: "12px",
            marginTop: "28px",
            flexWrap: "wrap",
          }}
        >
          <a
            href="?path=/docs/design-system-introduction--docs"
            style={{
              display: "inline-flex",
              alignItems: "center",
              gap: "8px",
              padding: "10px 18px",
              borderRadius: "8px",
              background: "#ffffff",
              color: "#c2410c",
              fontWeight: 700,
              fontSize: "14px",
              textDecoration: "none",
              boxShadow: "0 1px 2px rgba(0,0,0,0.10)",
            }}
          >
            Introduction を読む →
          </a>
          <a
            href="?path=/docs/design-system-tokens--docs"
            style={{
              display: "inline-flex",
              alignItems: "center",
              gap: "8px",
              padding: "10px 18px",
              borderRadius: "8px",
              background: "transparent",
              color: "#ffffff",
              fontWeight: 600,
              fontSize: "14px",
              border: "1px solid rgba(255,255,255,0.5)",
              textDecoration: "none",
            }}
          >
            Tokens を見る
          </a>
          <a
            href="?path=/docs/design-system-components--docs"
            style={{
              display: "inline-flex",
              alignItems: "center",
              gap: "8px",
              padding: "10px 18px",
              borderRadius: "8px",
              background: "transparent",
              color: "#ffffff",
              fontWeight: 600,
              fontSize: "14px",
              border: "1px solid rgba(255,255,255,0.5)",
              textDecoration: "none",
            }}
          >
            Components 一覧
          </a>
        </div>
      </div>
    </div>

    {/* ============ Metrics ============ */}
    <section style={{ marginTop: "40px" }}>
      <h2
        style={{
          fontSize: "22px",
          fontWeight: 700,
          margin: "0 0 16px",
          letterSpacing: "-0.005em",
        }}
      >
        Design System Metrics
      </h2>
      <div
        style={{
          display: "grid",
          gridTemplateColumns: "repeat(4, 1fr)",
          gap: "12px",
        }}
      >
        {[
          { label: "UI Primitives", value: "21", sub: "src/components/ui/" },
          { label: "Composite Components", value: "18", sub: "src/components/" },
          { label: "MDX Documents", value: "10", sub: "src/stories/design-system/" },
          { label: "Stories", value: "35+", sub: "21 ui + 14 composite" },
          { label: "Status Colors", value: "8", sub: "open / full / waitlist ..." },
          { label: "Themes", value: "2", sub: "light + dark" },
          { label: "Token Layers", value: "3", sub: "primitive / semantic / theme" },
          { label: "WCAG", value: "AA", sub: "axe-core 自動走査" },
        ].map((m) => (
          <div
            key={m.label}
            style={{
              padding: "18px 16px",
              border: "1px solid #e5e7eb",
              borderRadius: "10px",
              background: "#ffffff",
              boxShadow: "0 1px 1px rgba(0,0,0,0.02)",
            }}
          >
            <div
              style={{
                fontSize: "11px",
                fontWeight: 700,
                letterSpacing: "0.04em",
                textTransform: "uppercase",
                color: "#6b7280",
              }}
            >
              {m.label}
            </div>
            <div
              style={{
                fontSize: "30px",
                fontWeight: 800,
                margin: "6px 0 2px",
                color: "#c2410c",
                lineHeight: 1.1,
              }}
            >
              {m.value}
            </div>
            <div style={{ fontSize: "12px", color: "#4b5563" }}>{m.sub}</div>
          </div>
        ))}
      </div>
    </section>

    {/* ============ Read first ============ */}
    <section style={{ marginTop: "40px" }}>
      <h2
        style={{
          fontSize: "22px",
          fontWeight: 700,
          margin: "0 0 16px",
        }}
      >
        最初に読むべき 3 ページ
      </h2>
      <div
        style={{
          display: "grid",
          gridTemplateColumns: "repeat(3, 1fr)",
          gap: "16px",
        }}
      >
        {[
          {
            no: "01",
            title: "Introduction",
            desc:
              "デザインシステムのねらい、トークン 3 階層構造、参照すべきページの見取り図。",
            href: "?path=/docs/design-system-introduction--docs",
          },
          {
            no: "02",
            title: "Tokens",
            desc:
              "primitive (生の値) → semantic (意味別エイリアス) → theme (light/dark) の関係。",
            href: "?path=/docs/design-system-tokens--docs",
          },
          {
            no: "03",
            title: "Components",
            desc:
              "21 primitives と 18 composite components の早見表。Storybook 内リンク付き。",
            href: "?path=/docs/design-system-components--docs",
          },
        ].map((c) => (
          <a
            key={c.no}
            href={c.href}
            style={{
              display: "block",
              padding: "20px",
              border: "1px solid #e5e7eb",
              borderRadius: "10px",
              background: "#ffffff",
              textDecoration: "none",
              color: "inherit",
              boxShadow: "0 1px 1px rgba(0,0,0,0.02)",
            }}
          >
            <div
              style={{
                fontSize: "11px",
                fontWeight: 700,
                letterSpacing: "0.08em",
                color: "#c2410c",
              }}
            >
              STEP {c.no}
            </div>
            <div
              style={{
                fontSize: "18px",
                fontWeight: 700,
                margin: "6px 0 8px",
              }}
            >
              {c.title}
            </div>
            <div style={{ fontSize: "13px", color: "#4b5563", lineHeight: 1.6 }}>
              {c.desc}
            </div>
            <div
              style={{
                marginTop: "12px",
                fontSize: "12px",
                fontWeight: 600,
                color: "#c2410c",
              }}
            >
              読む →
            </div>
          </a>
        ))}
      </div>
    </section>

    {/* ============ Quick links ============ */}
    <section style={{ marginTop: "40px" }}>
      <h2 style={{ fontSize: "22px", fontWeight: 700, margin: "0 0 16px" }}>
        クイックリンク
      </h2>
      <div
        style={{
          display: "grid",
          gridTemplateColumns: "repeat(2, 1fr)",
          gap: "20px",
        }}
      >
        <div
          style={{
            padding: "20px",
            border: "1px solid #e5e7eb",
            borderRadius: "10px",
            background: "#ffffff",
          }}
        >
          <h3 style={{ margin: "0 0 12px", fontSize: "16px", fontWeight: 700 }}>
            Design System ドキュメント (MDX)
          </h3>
          <ul style={{ margin: 0, padding: 0, listStyle: "none", lineHeight: 2 }}>
            {[
              ["Introduction", "?path=/docs/design-system-introduction--docs"],
              ["Tokens", "?path=/docs/design-system-tokens--docs"],
              ["Colors", "?path=/docs/design-system-colors--docs"],
              ["Typography", "?path=/docs/design-system-typography--docs"],
              ["Spacing", "?path=/docs/design-system-spacing--docs"],
              ["Radius / Shadow / Z-index", "?path=/docs/design-system-radius-shadow-z-index--docs"],
              ["Icons", "?path=/docs/design-system-icons--docs"],
              ["Components", "?path=/docs/design-system-components--docs"],
              ["Accessibility", "?path=/docs/design-system-accessibility--docs"],
              ["Dark Mode", "?path=/docs/design-system-dark-mode--docs"],
              ["Component Checklist", "?path=/docs/design-system-component-checklist--docs"],
            ].map(([label, href]) => (
              <li key={label}>
                <a
                  href={href}
                  style={{ color: "#005d8c", textDecoration: "none", fontSize: "14px" }}
                >
                  {label}
                </a>
              </li>
            ))}
          </ul>
        </div>
        <div
          style={{
            padding: "20px",
            border: "1px solid #e5e7eb",
            borderRadius: "10px",
            background: "#ffffff",
          }}
        >
          <h3 style={{ margin: "0 0 12px", fontSize: "16px", fontWeight: 700 }}>
            主要コンポーネント Story
          </h3>
          <ul style={{ margin: 0, padding: 0, listStyle: "none", lineHeight: 2 }}>
            {[
              ["UI / Button", "?path=/docs/ui-button--docs"],
              ["UI / Input", "?path=/docs/ui-input--docs"],
              ["UI / Card", "?path=/docs/ui-card--docs"],
              ["UI / Dialog", "?path=/docs/ui-dialog--docs"],
              ["Atoms / EventStatusBadge", "?path=/docs/atoms-eventstatusbadge--docs"],
              ["Atoms / TagPill", "?path=/docs/atoms-tagpill--docs"],
              ["Molecules / Breadcrumb", "?path=/docs/molecules-breadcrumb--docs"],
              ["Molecules / Pagination", "?path=/docs/molecules-pagination--docs"],
              ["Molecules / SearchBox", "?path=/docs/molecules-searchbox--docs"],
              ["Organisms / Header", "?path=/docs/organisms-header--docs"],
              ["Organisms / EventCard", "?path=/docs/organisms-eventcard--docs"],
              ["Organisms / EventListRow", "?path=/docs/organisms-eventlistrow--docs"],
              ["Organisms / Footer", "?path=/docs/organisms-footer--docs"],
            ].map(([label, href]) => (
              <li key={label}>
                <a
                  href={href}
                  style={{ color: "#005d8c", textDecoration: "none", fontSize: "14px" }}
                >
                  {label}
                </a>
              </li>
            ))}
          </ul>
        </div>
      </div>
    </section>

    {/* ============ Footer note ============ */}
    <section
      style={{
        marginTop: "40px",
        padding: "20px",
        borderRadius: "10px",
        background: "#fff7ed",
        border: "1px solid #fed7aa",
        fontSize: "13px",
        color: "#7c2d12",
      }}
    >
      <strong style={{ display: "block", marginBottom: "4px", fontSize: "14px" }}>
        このカタログについて
      </strong>
      本サイトは <code>pnpm storybook</code> で起動する Storybook 10 のドキュメント
      モードです。サイドバーの順序は{" "}
      <strong>Welcome → Design System → UI → Components</strong> に固定されています
      (<code>.storybook/main.ts</code> の <code>storySort</code> 参照)。
      実装の正は <code>src/styles/tokens.css</code> /{" "}
      <code>src/styles/themes/&#123;light,dark&#125;.css</code>{" "}
      および <code>docs/design-system.md</code> です。
    </section>
  </div>
</Unstyled>
