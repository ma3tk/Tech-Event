{
  "name": "@tech-event/worker",
  "private": true,
  "version": "0.1.0",
  "description": "BullMQ worker for tech-event (participation / notification / lottery)",
  "main": "dist/apps/worker/apps/worker/src/main.js",
  "scripts": {
    "start": "node ../../dist/apps/worker/apps/worker/src/main.js",
    "dev": "tsx watch src/main.ts",
    "build": "tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "bullmq": "*",
    "ioredis": "*",
    "pino": "*"
  }
}
