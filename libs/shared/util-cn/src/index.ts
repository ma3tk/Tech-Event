/**
 * `@tech-event/shared-util-cn` — Tailwind class 合成ヘルパ `cn(...)` のみ。
 *
 * `clsx` と `tailwind-merge` を組み合わせて条件付き / 重複した Tailwind クラスを
 * 安全に結合する。プロジェクト全体で `cn(...)` の単一実装としてここを参照する。
 */
export * from "./cn";
