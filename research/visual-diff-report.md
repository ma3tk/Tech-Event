# 視覚差分レポート: tech-event クローン vs connpass 本家

> **※ このレポートは初版 (9 ペア時点) の archival です。最新版は [`visual-diff-final-report.md`](./visual-diff-final-report.md) を参照してください。**
> 最新版では新規 7 ページ (discover / calendar/[slug] / calendars / bookmarks / notifications / user-timeline / group-timeline) を加えた合計 **16 ペア (connpass) + 13 ペア (Luma)** の評価と、コンポーネント別カバレッジ更新まで含まれています。

---

- 比較日時: 2026-06-04
- ビューポート: 1280 x 1600 (Playwright `chromium-desktop`)
- スクリーンショット: `screenshots/clone/*.png`, `screenshots/connpass/*.png`
- 計測ペア数: 9 (top / explore / ranking / login / series / event-detail / group-detail / user-profile / signup)
- 評価軸: レイアウト構造 / 色合い / フォントサイズ感 / 情報密度 (各 ★★★★★ 満点)

評価軸の意味:

- レイアウト構造類似度: グリッド・カラム配置・主要セクションの並びの一致度
- 色合い類似度: ブランドカラー (アクセント色) / 背景色 / テキスト色の印象の近さ
- フォントサイズ感類似度: 見出し / 本文 / メタ情報の相対的なサイズ感
- 情報密度類似度: 1 ファーストビュー (1280x800) あたりに収まる情報量の近さ

---

## 1. `top` (トップページ)

| 軸 | 評価 |
| --- | --- |
| レイアウト構造 | ★★★☆☆ |
| 色合い | ★★★☆☆ |
| フォントサイズ感 | ★★★☆☆ |
| 情報密度 | ★★★☆☆ |

### Clone 固有の欠落要素

- 本家トップにある「3 ステップ説明 (会員になる/イベントを探す/イベントを管理する) のイラスト付きカード」のイラスト
- 本家のヒーロー右上にある「会員登録」CTA バナー (アクセントカラー赤 + コピー)
- 本家にある「機能紹介 / 利用事例 / 検索ヘルプ」カードのスクリーンショット入りビジュアル
- 右サイドカラム (`#sub`) の「最近見たイベント / イベントカレンダー」固定ペイン

### Clone 固有の余分要素

- ページ上部の検索ボックスが本家より大幅に大きい・色が明るい (オレンジアクセント)
- 「グループ別の新着」セクションが本家トップにはなく Clone 独自構成
- 「タグから探す」のピル集が本家より目立つフルワイドで表示

### 次に直すなら最優先の差分

1. ヒーローを連続 2 段の白背景 + 細い赤帯のスタイルに揃え、見出しを 28px 程度まで縮小
2. メインカラムを 2 カラム (主 / 右サイド固定) に変更し、右に「会員登録」「最近見たイベント」を配置
3. 3 ステップ説明のカードを画像入りビジュアル (赤系) に置き換える

---

## 2. `explore` (イベントを探す)

| 軸 | 評価 |
| --- | --- |
| レイアウト構造 | ★★★★☆ |
| 色合い | ★★★☆☆ |
| フォントサイズ感 | ★★★★☆ |
| 情報密度 | ★★★★☆ |

### Clone 固有の欠落要素

- 本家にある絞り込みパネル右側の広告枠 / バナー (Claude Code 広告など)
- 「並び替え: 新着 / 人気 / 開催日」のセグメントタブ (Clone はソート select 1 個のみ)
- イベントカードのタイトル横にある「主催グループのロゴ (小)」表示
- 「動画あり」「資料あり」「キャンセル待ち可」などのフィルタチップ

### Clone 固有の余分要素

- 左サイドの絞り込みパネルがフィールド数 6 件以上で本家 (4 件) よりリッチ
- 各カードに表示されているタグピル数 (Clone は最大 4 つ、本家は 0〜2)
- カード右端のサムネイル比率 (Clone は正方形気味、本家は 16:9)

### 次に直すなら最優先の差分

1. カードのサムネイル比率を 16:9 に揃え、左サイドフィルタの幅を 220px から 200px に縮小
2. ヘッダ直下のタブ (新着 / 人気) を追加して情報アーキテクチャを本家に揃える
3. 右サイドに広告 / おすすめバナー枠を追加して見た目の重心を本家側に寄せる

---

## 3. `ranking` (人気イベントランキング)

| 軸 | 評価 |
| --- | --- |
| レイアウト構造 | ★★★★☆ |
| 色合い | ★★★☆☆ |
| フォントサイズ感 | ★★★☆☆ |
| 情報密度 | ★★★☆☆ |

### Clone 固有の欠落要素

- 本家にある「年/月切り替えタブ」のスライド式 UI (Clone は固定タブのみ)
- 本家のランキングカード左の「金 / 銀 / 銅 メダル風アイコン」(Clone は黄系四角の枠付き番号)
- 右サイドの広告 / おすすめイベント (常時表示)

### Clone 固有の余分要素

- 各行のサムネイル右に「参加人数 / 開催日」を 2 段で表示 (本家は 1 行のメタ情報)
- 行ごとのカード境界線が濃く、本家より行間が広い

### 次に直すなら最優先の差分

1. 上位 3 件にだけ金/銀/銅メダルアイコンを当て、4 位以降は数字のみのシンプル表記に変更
2. 行高を 96px → 64px に圧縮し本家の情報密度に寄せる
3. 右サイドカラムを追加 (広告 + おすすめイベント)

---

## 4. `login` (ログイン)

| 軸 | 評価 |
| --- | --- |
| レイアウト構造 | ★★★★☆ |
| 色合い | ★★☆☆☆ |
| フォントサイズ感 | ★★★☆☆ |
| 情報密度 | ★★★☆☆ |

### Clone 固有の欠落要素

- 本家のヘッダにある赤帯 (`#connpass` ロゴ + 検索バーが赤背景)
- 「connpass アカウントでログイン」と「外部アカウントで登録/ログイン」の 2 カラム並置 (Clone は 1 カラム縦並び)
- パスワード再設定リンクの位置 (本家は灰色 ※テキスト、Clone は青リンク)

### Clone 固有の余分要素

- ページ下部の「開発用: シードユーザーでログイン」セクション (DEV 専用機能で本家には存在しない / 妥当)
- 「ログイン状態を保持する」チェックボックス (本家はチェックボックス非表示・常時保持)

### 次に直すなら最優先の差分

1. ログインフォームを左 / 外部認証ボタンを右の 2 カラム構成に変更
2. ヘッダのカラーリングを赤系のブランドカラーに統一 (現在のオレンジから赤寄りへ)
3. 「ログイン状態を保持する」を非表示 or "ログイン情報を記憶する" の控えめ表現に

---

## 5. `series` (グループ / シリーズ一覧)

| 軸 | 評価 |
| --- | --- |
| レイアウト構造 | ★★☆☆☆ |
| 色合い | ★★★☆☆ |
| フォントサイズ感 | ★★★☆☆ |
| 情報密度 | ★★☆☆☆ |

### Clone 固有の欠落要素

- 本家のグループ一覧の「縦長 1 カラムリスト + 右サイドの広告 / おすすめ」構成
- 各グループ右側の「フォロー数バッジ」「フォローボタン」
- 本家にあるカテゴリフィルタ (左カラム) — Clone は検索ボックスとタブのみ

### Clone 固有の余分要素

- 2 カラムのグリッドレイアウト (本家は 1 カラム)
- 各カードに表示されている長文の説明 (本家は 1 行)
- カバー画像の表示 (本家はロゴのみ、コンパクト)

### 次に直すなら最優先の差分

1. 2 カラムグリッドを 1 カラムリストに変更し、カードあたりの縦高さを 50% 削減
2. カバー画像を非表示 or 60px 程度の小サムネイルに格下げ、ロゴ中心の構成に
3. 右サイドカラムを追加 (おすすめ広告 / カテゴリ絞り込み)

---

## 6. `event-detail` (イベント詳細)

| 軸 | 評価 |
| --- | --- |
| レイアウト構造 | ★★★★☆ |
| 色合い | ★★☆☆☆ |
| フォントサイズ感 | ★★★☆☆ |
| 情報密度 | ★★★☆☆ |

### Clone 固有の欠落要素

- 本家上部の大型ヒーロー画像 (タイトル文字をオーバーレイ表示する青基調のバナー)
- 右サイドの「グループ」カードに表示される「主催者プロフィール / 過去イベント」のフィード
- 本家にある「会場の地図 (Google Maps 埋め込み)」(Clone は座標表記のみ)
- 「申し込み状況」セクションの参加者一覧プレビュー (本家は最初の 6 人を縦リストで表示)

### Clone 固有の余分要素

- 申し込みボックス右上に表示される「参加枠」のオレンジ系ボタン (本家は赤)
- 「コメント」が大きなセクションとしてメインカラムに常時表示 (本家はフィード扱いで小さく下部)
- 「タグ」セクションが見出しレベルの大きさで表示 (本家はフッタ近くで小さく)

### 次に直すなら最優先の差分

1. タイトルを画像オーバーレイ式のヒーローに変更し、タイトル下にカテゴリ帯 (青系) を配置
2. アクセント色をオレンジから本家の赤系 (`#ed1c24` 系) に変更
3. 会場セクションに静的マップ画像を追加 (Leaflet タイル / OSM 等で代替)

---

## 7. `group-detail` (グループ詳細)

| 軸 | 評価 |
| --- | --- |
| レイアウト構造 | ★★★☆☆ |
| 色合い | ★★★☆☆ |
| フォントサイズ感 | ★★★☆☆ |
| 情報密度 | ★★☆☆☆ |

### Clone 固有の欠落要素

- 本家にある左カラム固定の「グループプロフィール (ロゴ / フォロー数 / アクション)」
- 右サイドの「メンバー一覧 (アバターのみ大量並び)」グリッド
- 本家ヘッダにある「フォローする」赤ボタン (Clone はオレンジボタン「メンバーになる」)
- 開催予定イベントカードの右側に表示される「参加可否ラベル」

### Clone 固有の余分要素

- ページ上部の大きなカバー画像 (本家は背景帯のみで写真は使わない)
- 「グループについて」セクションが Markdown レンダリングで本家より長い
- 「管理者」セクションが大きく表示 (本家は小さなテキストリンク 1 行)

### 次に直すなら最優先の差分

1. ヒーローのカバー画像を高さ 80px の薄いブランド帯に置き換え、ロゴ + 主要数値 (フォロー/イベント数) を強調
2. メインカラムを 3 カラム (左: グループ情報 / 中: イベント一覧 / 右: メンバー) に変更
3. アクション CTA を赤に変更し、文言を「フォローする」に統一

---

## 8. `user-profile` (ユーザープロフィール)

| 軸 | 評価 |
| --- | --- |
| レイアウト構造 | ★★★☆☆ |
| 色合い | ★★★☆☆ |
| フォントサイズ感 | ★★★☆☆ |
| 情報密度 | ★★★☆☆ |

### Clone 固有の欠落要素

- 本家のプロフィール直下にある「所属グループのロゴ横並び」(横スクロール風の塊)
- 右サイドの「参加予定イベント」ミニカード集
- 本家にある「日付タイムライン UI」(参加履歴を左に日付帯 / 右にイベントカード)
- 「フォロー / フォロワー数」表示

### Clone 固有の余分要素

- 「主催イベント (10 件)」セクションを大きなカードリスト形式で表示 (本家は数値のみ)
- 「発表資料 (0 件)」セクションが空でも見出し表示
- カバー画像なしのコンパクトプロフィールヘッダ (本家はアバター + 自己紹介の 1 行型と異なる詰め方)

### 次に直すなら最優先の差分

1. 参加履歴を時系列タイムライン (日付帯付き) UI に変更
2. プロフィールヘッダ右に「フォロー/フォロワー数」と CTA「メッセージを送る」を追加
3. 主催イベント一覧を縦長 1 カラムリストへ縮小し、各行 48px 程度のメタ情報行に

---

## 9. `signup` (新規登録)

| 軸 | 評価 |
| --- | --- |
| レイアウト構造 | ★★★☆☆ |
| 色合い | ★★☆☆☆ |
| フォントサイズ感 | ★★★☆☆ |
| 情報密度 | ★★★☆☆ |

### Clone 固有の欠落要素

- 本家上部の「3 ステップ進捗バー (1. 情報を入力 → 2. アカウント連携 → 3. 完了)」
- 「※サイト ID としても利用されます。」「※半角英数字」などの細かなインライン補足
- ヘッダ赤帯 / 「新着イベント」ナビ

### Clone 固有の余分要素

- 外部認証 (X / Facebook / GitHub) ボタンをページ上部に大きく 3 段で配置 (本家にはなし)
- 「またはメールで登録」セパレータ
- 「利用規約 と プライバシーポリシーに同意します」が控えめなチェックボックス (本家はリンクと注意書きで強調)

### 次に直すなら最優先の差分

1. 上部に 3 ステップの進捗インジケータを実装
2. 外部認証を 1 ブロックにまとめ、本家同様にコンパクトに配置
3. ヘッダの赤帯を導入し、サイト全体のブランドカラーを赤系に統一

---

## 横断的に最優先で対応すべき改善

各ページ共通で改善余地が大きい順:

1. **ブランドカラー: オレンジ → 赤系へ変更**
   - 本家 connpass のアクセントカラーは赤 (`#ed1c24` 近傍)。Clone は `brand-orange` (`#ff5e2c` 近傍)。
   - これだけで色合い類似度が全ページで ★1〜2 上がる見込み。
2. **右サイドカラムの追加 (主要 5 ページ)**
   - top / explore / ranking / series / event-detail / group-detail / user-profile に共通で「広告 + 補助情報」の右カラムを追加。
   - 1 カラム中心の現状から本家の 2 カラム構成に揃えるだけで構造類似度が大きく改善。
3. **情報密度の引き締め**
   - カードの padding を 1.0rem → 0.5rem、行高さを 96px → 64px 等に圧縮。
   - 本家は「狭い縦間隔に情報を詰める」設計思想なので、Clone の余白多めの設計を引き締める。
4. **トップページのヒーロー構成見直し**
   - 大型コピー + 3 ステップ説明 (イラスト付き) + 会員登録 CTA の 1 stack 構成を再現。

---

## Luma 比較

- 比較日時: 2026-06-04
- ビューポート: 1280 x 1600 (Playwright `chromium-desktop`)
- スクリーンショット: `screenshots/luma/*.png`, `screenshots/clone/<name>-luma.png`
- 計測ペア数: 6 (top / discover / event-detail / calendar / user-profile / signin)
- Luma 固有メモ: `luma.com/ai-tinkerers` および `luma.com/peterm` は 404 だったため、`event-detail` と `user-profile` の Luma 側は実質 "404 イラスト + ヘッダ" のキャプチャになっている。他URLでも公開状態に依存するため、評価はヘッダ・フッタ・全体トーンを中心に行う。

評価軸の意味:

- レイアウト構造類似度: グリッド・カラム配置・主要セクションの並びの一致度
- 色合い類似度: ブランドカラー (アクセント色) / 背景色 / テキスト色の印象の近さ
- フォントサイズ感類似度: 見出し / 本文 / メタ情報の相対的なサイズ感
- 情報密度類似度: 1 ファーストビュー (1280x800) あたりに収まる情報量の近さ

---

### L1. `top` (トップページ)

| 軸 | 評価 |
| --- | --- |
| レイアウト構造 | ★☆☆☆☆ |
| 色合い | ★☆☆☆☆ |
| フォントサイズ感 | ★★☆☆☆ |
| 情報密度 | ★☆☆☆☆ |

#### Clone 不足要素 (Luma 起点)

- センター寄せ大型ヒーローコピー (3D iPhone モックアップ + 「素敵なイベントはここから始まる」)
- 余白を多くとった ultra-minimal な構成 (Luma はトップに情報を詰めない / Clone は新着・グループ・タグを縦に積む)
- フッタの「luma /カイブ / アプリ / ヘルプ」風の極小ナビゲーション
- パステル系グラデーション背景

#### 優先差分

1. (clone は connpass 寄せが基本方針のため)Luma 風に振るならヒーローのみ残し新着リストを 1 fold 下げる
2. Luma 流に背景にパステルグラデを当てる場合、ブランドカラーの両立を別途検討する必要あり
3. フォントは現状でほぼ Luma と近い ( Inter 系 sans-serif )

---

### L2. `discover` (発見・探す)

| 軸 | 評価 |
| --- | --- |
| レイアウト構造 | ★★☆☆☆ |
| 色合い | ★★☆☆☆ |
| フォントサイズ感 | ★★★☆☆ |
| 情報密度 | ★★☆☆☆ |

#### Clone 不足要素 (Luma 起点)

- 「人気のイベント」を 2 列タイル (大きめサムネ + 短いタイトル) で並べる構成
- 「カテゴリから探す」のラージカード (アイコン + カテゴリ名 + イベント件数)
- 「おすすめのカレンダー」セクション (シティ別 / コミュニティ別のフォローボタン付きリスト)
- 「地域のイベントをさがす」グローバル都市グリッド (アイコンつき)

#### 優先差分

1. discover 系で **カテゴリカード化** を導入 (現状のテキストリンクからアイコン付きカードへ)
2. 「都市・地域から探す」をタグセクションの代替として追加 (connpass の「都道府県」と統合可)
3. 検索フォームと並列に「人気イベント横スクロール枠」を追加

---

### L3. `event-detail` (イベント詳細)

| 軸 | 評価 |
| --- | --- |
| レイアウト構造 | ★☆☆☆☆ (Luma 404 のため形状比較不能) |
| 色合い | ★★☆☆☆ |
| フォントサイズ感 | ★★★☆☆ |
| 情報密度 | ★☆☆☆☆ |

#### Clone 不足要素 (Luma 起点 / 一般的なLumaイベント詳細を踏まえる)

- 左カラムに大判ヒーロー画像 + 装飾色を抽出した背景グラデーション
- 「Register / Approval Required」CTA を 1 つの大きなボタンに集約
- 主催者プロフィール (アバター + フォローボタン) を CTA すぐ隣に固定
- スクロールしてもヘッダにイベントタイトル + 申込ボタンが追従する mini-CTA

#### 優先差分

1. ヒーロー画像から主要色を抽出してページ背景にバインドする仕組み
2. 申込ボタンを 1 fold 内に集約 (現在の Clone はサイドバー右上に小さく配置)
3. 主催者情報 + フォローボタンを CTA の直下に追加 (現在の Clone は最下部寄り)

---

### L4. `calendar` (カレンダー / 都市)

| 軸 | 評価 |
| --- | --- |
| レイアウト構造 | ★★★☆☆ |
| 色合い | ★★☆☆☆ |
| フォントサイズ感 | ★★★☆☆ |
| 情報密度 | ★★★☆☆ |

#### Clone 不足要素 (Luma 起点)

- ページ最上部の都市のヒーロー写真 (東京なら桜 + スカイツリー、フルワイド)
- 「東京」のような都市名 + フォローボタン + メール購読のミニフォーム
- 「今週 / 6月10日」のような日付見出しで自動セクション分割するタイムライン
- 静的マップ (右サイド) と関連カレンダー (Startup Calendar など) のリスト

#### 優先差分

1. グループ詳細ページに「日付見出しでイベントをグルーピングする」UI を導入
2. 右サイドに静的マップ + 関連グループフォローリストを追加
3. ヘッダーカバー画像を季節 / 地域写真ライクな雰囲気にする (テック特化なのでホワイト維持でもOK、優先度は中)

---

### L5. `user-profile` (ユーザープロフィール)

| 軸 | 評価 |
| --- | --- |
| レイアウト構造 | ★★☆☆☆ (Luma 404 のため形状比較不能) |
| 色合い | ★★☆☆☆ |
| フォントサイズ感 | ★★★☆☆ |
| 情報密度 | ★★☆☆☆ |

#### Clone 不足要素 (Luma 起点 / 公開Lumaプロフィールを踏まえる)

- 大きな丸アバター + 一行自己紹介 + ソーシャルリンクのアイコン列をセンタリング
- 「Hosting / Going / Hosted」の 3 タブ切替 + タブ下に大判イベントカード
- フォロー数 / フォロワー数のチップ
- カバー画像なし (Luma は基本背景白 + パステル装飾のみ)

#### 優先差分

1. プロフィールヘッダをセンタリング型に変更 (現在は左寄せ)
2. 参加履歴 / 主催イベントタブ切替を実装
3. ソーシャルリンク (X / GitHub / Web) のアイコン列を追加

---

### L6. `signin` (サインイン)

| 軸 | 評価 |
| --- | --- |
| レイアウト構造 | ★★★☆☆ |
| 色合い | ★★☆☆☆ |
| フォントサイズ感 | ★★★★☆ |
| 情報密度 | ★★★★☆ |

#### Clone 不足要素 (Luma 起点)

- 中央寄せの薄い角丸カード + 周囲がパステルグラデの空間
- ロゴアイコン + 「ようこそ」短文 1 行のみ (Clone はフォーム要素が多い)
- メールフィールドの右上に「電話番号を使用」のテキストリンクトグル
- 「Google でサインイン」「Passkey でサインイン」の 2 種類のみ (SNS 連携を絞っている)
- パスワードフィールドが無い (Luma はメール = Magic Link 一本)

#### 優先差分

1. サインインのコア導線をメール (Magic Link) 一本化、パスワードログインを副セクションに格下げ → Luma 同等のシンプルさへ
2. 中央カード周辺をホワイトスペースで満たし、開発用シードユーザー欄を `<details>` でたたむ
3. Passkey ログインを追加 (現在の Clone には未実装)

---

## 3者比較サマリー

### 類似度推定スコア (Clone → 各本家)

| ペア | vs connpass | vs Luma | 備考 |
| --- | --- | --- | --- |
| top | 約 55% | 約 25% | connpass の「左メイン + 右サイドカラム + 縦長新着リスト」が骨格は近い。Luma とはほぼ別物 |
| explore / discover | 約 70% | 約 35% | カード + 左フィルタは connpass 寄り。Luma のカテゴリカード/都市グリッドは未対応 |
| event-detail | 約 65% | 約 20% | connpass のセクション構成は再現。Luma の大型ビジュアル CTA は未対応 |
| group-detail / calendar | 約 55% | 約 35% | カバー画像があるのが Luma 寄りだが、タイムライン化や都市ヒーローは未対応 |
| user-profile | 約 55% | 約 30% | connpass のタブ構成は近い。Luma のセンタリング型は未対応 |
| login / signin | 約 60% | 約 50% | SNS ボタンがあるのは Luma 寄りだが、パスワードがあるのは connpass 寄り。中間に位置 |
| **平均** | **約 60%** | **約 33%** | **本クローンは現状 connpass 寄せ実装** (狙い通り) |

### スコアリングの根拠

レイアウト構造 (40%) + 色合い (20%) + フォントサイズ感 (20%) + 情報密度 (20%) の重み付き平均で各ペア★5 → 100%に正規化。

---

## 次に取り組むべき Top10 差分 (全体俯瞰)

優先度高い順:

1. **ブランドカラー赤 (`#ed1c24` 系) への切替** — connpass 類似度 +10%、Luma 類似度には影響しないが本筋
2. **右サイドカラムの導入 (top / explore / ranking / event-detail / user-profile)** — connpass 類似度 +15%
3. **イベント詳細のヒーロー画像オーバーレイ化** — connpass 類似度 +5%、Luma 類似度 +10%
4. **discover に「カテゴリ大カード + 都市グリッド」追加** — Luma 類似度 +10% かつ connpass の「都道府県」とも統合可能
5. **情報密度の引き締め (カード padding / 行高さ圧縮)** — connpass 類似度 +5%、特に ranking / series
6. **signin の Magic Link 主軸化 + Passkey 追加** — Luma 類似度 +10%
7. **user-profile のタイムライン UI 化 + フォロー数表示** — 両者ともに +5%
8. **group-detail / calendar に「日付見出しで自動セクション分割」導入** — Luma 類似度 +5%
9. **トップヒーローの 3 ステップ説明 (イラスト付き) の実装** — connpass 類似度 +5%
10. **mini-CTA ヘッダ追従 (event-detail でスクロール時に申込ボタン上部固定)** — UX 改善 + Luma 寄せ

---

## 比較画像 / Triptych 一覧

`scripts/build-triptych.ts` で生成。

| ペア | ファイル |
| --- | --- |
| top | `screenshots/triptych/top.png` (connpass / clone / luma) |
| discover | `screenshots/triptych/discover.png` (connpass-explore / clone-explore / luma-discover) |
| event-detail | `screenshots/triptych/event-detail.png` (connpass / clone / luma 404 ※) |
| calendar | `screenshots/triptych/calendar.png` (connpass-group-detail / clone-group-detail / luma-tokyo) |
| user-profile | `screenshots/triptych/user-profile.png` (connpass / clone / luma 404 ※) |
| signin | `screenshots/triptych/signin.png` (connpass-login / clone-login / luma-signin) |

※ Luma 側 `event-detail` と `user-profile` は URL 公開状態に依存し、撮影時点では 404 ページが取得された。比較は他のページから読み取れる「Luma の一般的なデザイン特徴」を踏まえて行っている。
